# Sample NestJS Project

This is a minimal NestJS project with a single `GET /` endpoint.

## Scripts

- `npm run start:dev` - Run the app directly from TypeScript.
- `npm run build` - Compile TypeScript to `dist/`.
- `npm run start` - Run the compiled output.

## Endpoint

- `GET /` returns `Hello from NestJS!`.
